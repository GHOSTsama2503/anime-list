version: "2"
sql:
    - engine: "sqlite"
      queries: "database/sql/**/*.sql"
      schema: "database/sql/schema.sql"
      gen:
        go:
          package: queries
          out: database/queries
